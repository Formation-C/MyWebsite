#include "Templates.h"

//template<typename T>
//std::vector<T> Template::Filterobjects(function<T> validator) {
//    std::vector<T*> result;
//
//    std::vector<T*>::iterator iter, iter_end;
//    for (iter = T::objects.begin(), iter_end = T::objects.end(); iter != iter_end; ++iter) {
//        T* object = *iter;
//        if (validator(object)) {
//            result.push_back(object);
//        }
//    }
//    return result;
//}
